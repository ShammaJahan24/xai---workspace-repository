// Removed from the page — the design now keeps exactly the four required
// sections (Hero, Insight Flow, Workspace, Live Inference Field). Kept as a
// no-op module because this filesystem does not allow file deletion.
export {}
